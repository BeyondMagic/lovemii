#$menu = wofi --show drun --allow-images

bindl = , XF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle

# For volume (special keys).
bindl  = , XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
#bindle = , XF86AudioMute, exec, ags run-js 'indicator.popup(1);'

bindle = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
#bindle = , XF86AudioRaiseVolume, exec, ags run-js 'indicator.popup(1);'

bindle = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
#bindle = , XF86AudioLowerVolume, exec, ags run-js 'indicator.popup(1);'

# For brightnesss.
bindle = , XF86MonBrightnessUp, exec, brightnessctl set '10+'
#bindle = , XF86MonBrightnessUp, exec, ags run-js 'indicator.popup(1);'

#bindle = , XF86MonBrightnessDown, exec, ags run-js 'indicator.popup(1);'
bindle = , XF86MonBrightnessDown, exec, brightnessctl set '10-'

# Hyprland.
bind = Alt, delete, killactive,

# Toglg main window dwingle.
bind = Alt, P, pseudo,

# Toggle windows positioning dwingle.
bind = Alt, J, togglesplit,

# Move focus with mainMod + arrow keys
bind = Alt, left, movefocus, l
bind = Alt, right, movefocus, r
bind = Alt, up, movefocus, u
bind = Alt, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = Alt, 1, workspace, 1
bind = Alt, 2, workspace, 2
bind = Alt, 3, workspace, 3
bind = Alt, 4, workspace, 4
bind = Alt, 5, workspace, 5
bind = Alt, 6, workspace, 6
bind = Alt, 7, workspace, 7
bind = Alt, 8, workspace, 8
bind = Alt, 9, workspace, 9
bind = Alt, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = Alt SHIFT, 1, movetoworkspace, 1
bind = Alt SHIFT, 2, movetoworkspace, 2
bind = Alt SHIFT, 3, movetoworkspace, 3
bind = Alt SHIFT, 4, movetoworkspace, 4
bind = Alt SHIFT, 5, movetoworkspace, 5
bind = Alt SHIFT, 6, movetoworkspace, 6
bind = Alt SHIFT, 7, movetoworkspace, 7
bind = Alt SHIFT, 8, movetoworkspace, 8
bind = Alt SHIFT, 9, movetoworkspace, 9
bind = Alt SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = Alt, S, togglespecialworkspace, scratchpad
bind = Alt SHIFT, S, movetoworkspace, special:scratchpad

# Scroll through existing workspaces with mainMod + scroll
bind = Alt, mouse_down, workspace, e+1
bind = Alt, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = Alt, mouse:272, movewindow
bindm = Alt, mouse:273, resizewindow

# Cycle through programs.
bind = ALT, Tab, cyclenext,
bind = ALT, Tab, bringactivetotop,

# Toggle floating.
bind = Alt, V, togglefloating,
# bind = , tap:3, exec, hyprctl dispatch togglefloating
# 
# # Move windows with touch.
# bindm = , longpress:2, movewindow
# bindm = , longpress:3, resizewindow

# Kill Hyprland.
#bind = ShiftAltAlt, Q, exec, hyprctl kill

# Cycle through clipboard list.
bind = Alt, C, exec, pkill fuzzel || cliphist list | fuzzel --match-mode=fzf --dmenu | cliphist decode | wl-copy

# Terminal.
$terminal = foot
$_ = DBUS_SESSION_BUS_ADDRESS=unix:path=/tmp/dbus # Fix for non-systemd systems (DBUS_SESSION_BUS_ADDRESS not being set.)
bind = Alt, return, exec, $_ $terminal --window-size-pixels=100x100
bind = Alt, backspace, exec, $_ $terminal --title="foot floating"
bind = AltShift, backspace, exec, $_ $terminal --title="foot floating"
bind = ALT, backspace, exec, $_ $terminal --title="foot floating"

# Zoom-in application.
bind = Alt, Z, exec, $_ woomer

# Application launcher.
bind = Alt, D, exec, $_ fuzzel

# Partial screenshot.
bind = ,Print, exec, $_ flameshot gui --raw | wl-copy

# Full screenshot.
bind = Alt, Print, exec, $_ flameshot screen

# Colour picker.
bind = Alt SHIFT, Print, exec, $_ hyprpicker --autocopy

# Text-to-image processors.
# - Normal.
#bind = ControlAltShift,S,exec,grim -g "$(slurp -d -c D1E5F4BB -b 1B232866 -s 00000000)" "tmp.png" && tesseract "tmp.png" - | wl-copy && rm "tmp.png"

# - English.
#bind = AltShift,T,exec,grim -g "$(slurp -d -c D1E5F4BB -b 1B232866 -s 00000000)" "tmp.png" && tesseract -l eng "tmp.png" - | wl-copy && rm "tmp.png"

# - Japanese
#bind = AltShift,J,exec,grim -g "$(slurp -d -c D1E5F4BB -b 1B232866 -s 00000000)" "tmp.png" && tesseract -l jpn "tmp.png" - | wl-copy && rm "tmp.png"

# Media
#bind = AltShift, N, exec, playerctl next || playerctl position `bc <<< "100 * $(playerctl metadata mpris:length) / 1000000 / 100"`
#bindl= ,XF86AudioNext, exec, playerctl next || playerctl position `bc <<< "100 * $(playerctl metadata mpris:length) / 1000000 / 100"`
#bind = AltShift, B, exec, playerctl previous
#bind = AltShift, P, exec, playerctl play-pause
#bind = ,XF86AudioPlay, exec, playerctl play-pause

# Lock.
#bind = Alt, L, exec, swaylock

# Sleep.
#bind = AltShift, L, exec, loginctl suspend

# Hibernate.
#bind = AltShiftAlt, L, exec, doas hibernate

# Fullscreen.
bind = Alt, F, fullscreen, 0
bind = Alt, M, fullscreen, 1

# Window split ratio
binde = Alt, Minus, splitratio, -0.1
binde = Alt, Equal, splitratio, 0.1
binde = Alt, Semicolon, splitratio, -0.1
binde = Alt, Apostrophe, splitratio, 0.1

# Cycle through workspaces.
bind = ControlAlt, right, workspace, +1
bind = ControlAlt, left, workspace, -1

# Toggle virtual keyboard.
#bind = Alt, K, exec, ags -t 'osk'

# Toggle overview.
#bind = Alt, Tab, exec, ags -t 'overview'
#bind = Alt, D, exec, ags -t 'overview'

# Zoom out
#bind = Ctrl+Alt, mouse_down, exec, hyprctl keyword cursor:zoom_factor $(awk "BEGIN {print $(hyprctl getoption cursor:zoom_factor | grep 'float:' | awk '{print $2}') + 0.5}")

# Zoom in
#bind = Ctrl+Alt, mouse_up, exec, hyprctl keyword cursor:zoom_factor $(awk "BEGIN {print $(hyprctl getoption cursor:zoom_factor | grep 'float:' | awk '{print $2}') - 0.5}")

binde = SUPER, KP_Subtract, exec, hyprctl getoption cursor:zoom_factor | grep float | awk '{if($2!=1) system("hyprctl keyword cursor:zoom_factor " $2 - 0.1) }' && hyprctl getoption cursor:zoom_factor | grep float | awk '{print $2}' | xargs notify-send -t 1000
binde = SUPER, KP_Add, exec, hyprctl getoption cursor:zoom_factor | grep float | awk '{ system("hyprctl keyword cursor:zoom_factor " $2 + 0.1) }' && hyprctl getoption cursor:zoom_factor | grep float | awk '{print $2}' | xargs notify-send -t 1000
