# Hyprland plugin manager.
exec-once = hyprpm reload

exec-once = hypridle

exec-once = runsvdir ~/.local/services/

exec-once = sv once fcitx5

# Audio and video.
exec-once = sv once pipewire
exec-once = sv once pipewire-pulse
exec-once = sv once wireplumber

exec-once = sv once syncthing

#exec-once = sv once pueued

exec-once = sv once ssh-agent

exec-once = sv once swww

exec-once = sv once iio-hyprland
exec-once = sv once cliphist

exec-once = sv once ags
exec-once = sv once swaync

# Backlight automatic adjuster according to my surroundigs.
#exec-once = /usr/lib/geoclue-2.0/demos/agent
exec-once = gammastep

# Reload hyprpm
exec-once = hyprpm reload

# Authentatication
#exec-once = gnome-keyring-daemon --start --components=secrets
exec-once = sv once polkit-gnome-authentication-agent-1
